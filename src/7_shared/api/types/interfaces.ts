export interface LoginFormValues {
  email: string;
  password: string;
}

export interface SignFormValues {
  name: string;
  email: string;
  password: string;
  passwordConfirm: string;
}
